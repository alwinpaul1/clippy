{
  "version": "1.0.30",
  "notes": {
    "features": [],
    "improvements": [
      "Clippy now tells you when background sync has stopped, instead of showing a green light regardless",
      "Background sync restarts itself after an app update — no need to open Clippy to get it going again"
    ],
    "fixes": [
      "Made the background-sync fix stick: it now applies on its own, even if you don't open the app after updating",
      "Clips captured while the app was asleep no longer pile up unbounded on the phone"
    ]
  }
}
