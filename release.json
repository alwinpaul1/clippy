{
  "version": "1.0.34",
  "notes": {
    "features": [
      "Stable release on clippy.alwinpaul.me — self-hosted relay for all devices, no VPN required"
    ],
    "improvements": [
      "Open-source MIT license and SignPath-ready code signing policy for free Windows Authenticode",
      "Windows installer uses a permanent AppId and real AppVersion from the release",
      "In-app updates show full version+build labels",
      "Update checks retry briefly on DNS/network blips"
    ],
    "fixes": [
      "macOS in-app update now waits for Clippy to quit before swapping the app"
    ]
  }
}
